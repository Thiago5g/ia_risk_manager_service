services:
  ia-engine:
    build:
      context: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - MEMORY_PATH=/app/memory.json
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8000
